import Miner from "../index";

describe("miner", () => {
  let miner: any;

  beforeEach(() => {
    // miner = new Miner();
  });
  afterEach(() => {
    miner = null;
  });

  it("exists", () => {
    //console.log(miner);
  });
  it("mines a block", () => {
    //miner.mineBlock();
  });
});
